[package]
name = "brainwave"
version = "0.1.0"
edition = "2021"
description = "Local LLM inference stack manager"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "2", features = ["json"] }
colored = "2"
